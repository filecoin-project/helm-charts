{{- range $sec := .Values.application.secrets }}
{{- if not $sec.external }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ $.Release.Name }}-{{ $sec.name }}
data:
  {{- range $k, $v := .keys }}
  {{ $k }}: {{ $v | b64enc }}
  {{- end }}
{{- end }}
{{- end }}
